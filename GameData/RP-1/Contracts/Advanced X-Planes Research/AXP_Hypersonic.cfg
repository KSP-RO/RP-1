CONTRACT_TYPE
{
	name = AXP_Hypersonic
	group = AdvXPlanes

	title = Uncrewed Hypersonic Flight
	
	tag = exclude_Supersonic

	description = <b>Program: Advanced X-Plane Research<br>Type: <color=green>Required</color></b><br><br>Design, build, and fly an uncrewed jet aircraft to maintain @VesselGroup/HoldSituation/minSpeed m/s in level flight, then return home safely.
	genericDescription = Design, build, and fly an uncrewed jet aircraft to maintain a specific speed in level flight.

	synopsis = Fly an uncrewed jet aircraft to maintain @VesselGroup/HoldSituation/minSpeed m/s in level flight.

	completedMessage = Congratulations on a successful flight!
	
	sortKey = 4503

	deadline = 0
	cancellable = true
	declinable = true
	autoAccept = false
	minExpiry = 1
	maxExpiry = 1
	maxCompletions = 1
	maxSimultaneous = 1

	targetBody = HomeWorld()


	// ************ REWARDS ************
	prestige = Significant
	advanceFunds = 0
	rewardScience = 0
	rewardFunds = 0
	failureFunds = 0
	rewardReputation = (30 + @completions * 5) * @rewardFactor
	failureReputation = 0 // was @rewardReputation
	
	DATA
	{
		type = double
		antiGrindCompletion = $AXP_Hyper_Completion == 0 ? (UniversalTime() - @RP0:expectedDays_XPlanesSupersonicOptional * 86400) : $AXP_Hyper_Completion
	}
	
	DATA
	{
		type = int
		index = $RP0_AXP_Hyper_Difficulty
		completions = $RP0_AXP_Hyper_Difficulty
	}

	DATA
	{
		type = float
		elapsedDays = Round((UniversalTime() - @antiGrindCompletion) / 86400.0)
		rewardFactor = Log(Max(@elapsedDays / @RP0:expectedDays_XPlanesSupersonicOptional * 20 - 9, 1), 2) / 3.46
		rewardFactorPercent = Round(@rewardFactor * 100, 1)
	}
	
	// ************* REQUIREMENTS ****************

	REQUIREMENT
	{
		name = ProgramActive
		type = ProgramActive
		program = AdvXPlanes
	}
	
	BEHAVIOUR
	{
		name = IncrementTheCount 
		type = Expression

		CONTRACT_OFFERED
		{
			
			AXP_Hyper_Completion = ($AXP_Hyper_Completion + 0) == 0 ? (UniversalTime() - @RP0:expectedDays_XPlanesSupersonicOptional * 86400) : ($AXP_Hyper_Completion + 0)
		
		}
		
		CONTRACT_COMPLETED_SUCCESS
		{
			AXP_Hyper_Completion = UniversalTime()
		}
	}
	
	// Base contract uses a fixed speed and does not advance the repeatable
	// series difficulty (RP0_AXP_Hyper_Difficulty); the Repeatable contract
	// owns that counter so it starts at index 0.
	

	PARAMETER
	{
		name = VesselGroup
		type = VesselParameterGroup
		title = Maintain between @HoldSituation/minSpeed m/s and @HoldSituation/maxSpeed m/s in level flight with an uncrewed jet aircraft.
		define = supersonicCraft
		dissassociateVesselsOnContractCompletion = true
		resetChildrenWhenVesselDestroyed = true
		
		PARAMETER
		{
			name = BuiltAtSPH
			type = VesselBuiltAt
			builtAt = SPH
		}

		PARAMETER
		{
			name = NoCrew
			type = HasCrew
			minCrew = 0
			maxCrew = 0
			title = Uncrewed vessel
			hideChildren = true
		}

		PARAMETER
		{
			name = NoRocket
			type = PartValidation
			title = Vessel is a jet (no rocket engines allowed)
			hideChildren = true
			NONE
			{
				partModule = ModuleEnginesRF
				partModule = ModuleEnginesAJEPropeller
			}
		}
		
		PARAMETER
		{
			name = HoldSituation
			type = ReachState
			minSpeed = 1625
			maxSpeed = 1787.5
			minRateOfClimb = -10
			maxRateOfClimb = 10
			situation = FLYING
			
			title = Hold between @minSpeed m/s to @maxSpeed m/s in level flight.
			
			disableOnStateChange = true
			
			PARAMETER
			{
				name = Duration
				type = Duration
				duration = 3m
				preWaitText = Reach specified speed.
				waitingText = Testing highspeed flight
				completionText = Flight completed, you are cleared to land.
			}
		}
	}
}